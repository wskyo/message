当前分支是alps-release-o1.bsp-default

先创建一个本地分支mtk6739-o1-a3a-v5.0-driveonly

git checkout -b mtk6739-o1-a3a-v5.0-driveonly

再将这个提交推到mtk6739-o1-a3a-v5.0-driveonly

git push jgs HEAD:mtk6739-o1-a3a-v5.0-driveonly


创建远程分支

1. cd到git库下

	cd ~/public_html

2. 创建本地分支

	git checkout -b foo

3. 推送到远程

	git push origin foo

如下命令有相同的效果：

	git push upstream new_dev
	git push upstream new_dev:new_dev
	git push upstream new_dev:refs/heads/new_dev


删除远程分支

	git  push origin :foo

	git push origin --delete foo

